<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_accept_payment_wizard_form" model="ir.ui.view">
        <field name="name">custom.sale.order.accept.payment.wizard.form</field>
        <field name="model">custom.sale.order.accept.payment.wizard</field>
        <field name="arch" type="xml">
            <form string="Accept Payment">
                <sheet>
                    <group>
                        <group>
                            <field name="custom_sale_order_id" readonly="1"/>
                            <field name="partner_id" readonly="1"/>
                        </group>
                        <group>
                            <field name="amount_total" readonly="1"/>
                            <field name="currency_id" invisible="1"/>
                        </group>
                    </group>
                    <group>
                        <field name="payment_method" widget="radio"/>
                    </group>
                    <group invisible="payment_method != 'bank_transfer'">
                        <field name="utr_number"
                               placeholder="Enter UTR Number"
                               required="payment_method == 'bank_transfer'"/>
                    </group>
                    <group invisible="payment_method != 'online_payment'">
                        <field name="routed_provider" readonly="1"/>
                        <div colspan="2" class="alert alert-info" role="alert">
                            A secure payment link will be generated and emailed
                            to the customer. They can complete payment from any
                            device. (PayU / Stripe integration — Phase 2.2)
                        </div>
                    </group>
                </sheet>
                <footer>
                    <button name="action_confirm" string="Confirm"
                            type="object" class="btn-primary"/>
                    <button string="Cancel" class="btn-secondary" special="cancel"/>
                </footer>
            </form>
        </field>
    </record>
</odoo>
